<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <!-- 使用自定义的View实现TabLayout的条目 -->
    <com.ytempest.tablayoutpro.TabLayoutPro
        android:id="@+id/tab_layout"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@android:color/white"
        android:gravity="center"
        app:tabVisibleNum="4"
        app:layout_scrollFlags="scroll"
        app:tabGravity="center"
        app:tabIndicatorColor="@android:color/holo_blue_bright"
        app:tabIndicatorHeight="3.0dp"
        app:tabMode="scrollable"/>

    <!--   使用默认的 TabLayout 条目-->
    <!-- @style/CompatTabLayoutTextAppearance：该样式用于解决字体大小写以及字体大小问题 -->
   <!-- <com.ytempest.tablayoutpro.TabLayoutPro
        android:id="@+id/tab_layout"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@android:color/white"
        android:gravity="center"
        app:tabPaddingBottom="5dp"
        app:tabTextAppearance="@style/CompatTabLayoutTextAppearance"
        app:layout_scrollFlags="scroll"
        app:tabGravity="center"
        app:tabIndicatorColor="@android:color/holo_blue_bright"
        app:tabIndicatorHeight="3.0dp"
        app:tabMode="scrollable"
        app:tabSelectedTextColor="@android:color/holo_red_light"
        app:tabTextColor="@android:color/darker_gray" />-->

    <android.support.v4.view.ViewPager
        android:id="@+id/view_pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</LinearLayout>